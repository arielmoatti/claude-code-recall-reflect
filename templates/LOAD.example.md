# LOAD — <שם הפרויקט>

**לקובץ הזה קורא `/load` בתחילת כל שיחה.** ההוראות כאן מופנות ל-Claude — תן לו לקרוא קבצי הקשר ספציפיים לפני שהוא עונה לך.

---

## קרא עכשיו

Claude, קרא את הקבצים הבאים עכשיו כדי לטעון את ההקשר של הפרויקט:

### תיעוד בסיסי

- `README.md` — מה הפרויקט הזה בכלל

### זיכרון פרויקט (אם קיים)

- `.claude/memory/MEMORY.md` — אינדקס לכל הזיכרון הספציפי של הפרויקט
- `.claude/memory/REGISTRY.md` — IDs, credentials, webhooks, שמות תרחישים
- `.claude/memory/ARCH_PATTERNS.md` — תבניות אדריכליות ספציפיות ללקוח
- `.claude/memory/CURRENT_SPRINT.md` — מה עובדים עליו עכשיו
- `.claude/memory/negative-knowledge.md` — שגיאות מאומתות (מה לא לעשות)

### סקילים שהם רלוונטיים דווקא לפרויקט הזה

_(ערוך את הרשימה הזו — הפנה לסקילים גלובליים ב-`~/.claude/skills/` שנדרשים באופן קבוע לעבודה בפרויקט)_

- ~~`~/.claude/skills/<שם-סקיל>.md` — למתי זה רלוונטי~~

### חוקים ספציפיים לפרויקט (אם יש CLAUDE.md)

- `CLAUDE.md` בתיקיית השורש של הפרויקט

---

## מה לעשות אחרי הטעינה

דווח בעברית בקצרה:

> טעון — <שם הפרויקט>. [משפט קצר שמסכם את הסטטוס מ-`CURRENT_SPRINT.md` אם קיים]. מוכן לעבודה.

**אל תמשיך לשום משימה עד שהמשתמש נתן הוראה מפורשת.** `/load` הוא טעינה — לא התחלה של עבודה.

---

## הערות תחזוקה (לא ל-Claude — לך, המשתמש)

- ערוך את הקובץ הזה כשהזיכרון של הפרויקט משתנה — נוסף קובץ חדש? עדכן כאן את ההפניה.
- אל תצביע על קובץ שלא קיים — Claude ידווח שגיאה.
- אם הפרויקט הזה לא צריך `ARCH_PATTERNS.md` או `CURRENT_SPRINT.md` — מחק את השורה המתאימה. הרשימה הזו היא סלט פתוח.
